<Window x:Class="TruckScale.Pos.SignatureCaptureWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:wfi="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration"
        Title="Driver Signature"
        Width="620" Height="430"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- HOST OCULTO: aquí vive el SigPlusNET (WinForms control) -->
        <wfi:WindowsFormsHost x:Name="SigHost"
                              Width="1" Height="1"
                              Visibility="Collapsed"/>

        <!-- Encabezado -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <md:PackIcon Kind="Draw" Width="28" Height="28"
                         VerticalAlignment="Center" Foreground="#1565C0"/>
            <TextBlock Text="Driver Signature" FontSize="18" FontWeight="SemiBold"
                       VerticalAlignment="Center" Margin="8,0,0,0"/>
        </StackPanel>

        <!-- Info -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Ticket:" FontWeight="Bold" Margin="0,0,4,0"/>
            <TextBlock x:Name="TxtTicketNum" Margin="0,0,20,0"/>
            <TextBlock Text="Total:" FontWeight="Bold" Margin="0,0,4,0"/>
            <TextBlock x:Name="TxtTotal"/>
        </StackPanel>

        <!-- Preview WPF (como demo: nosotros pintamos el GetSigImage aquí) -->
        <Border Grid.Row="2"
        MinHeight="220"
        BorderBrush="#BDBDBD" BorderThickness="1" Background="#F5F5F5"
        CornerRadius="4">
            <Grid>
                <TextBlock x:Name="TxtWaiting"
                           Text="Sign on the pad…"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           Foreground="#9E9E9E" FontSize="14"/>
                <Image x:Name="SigPreview"
       Stretch="Uniform"
       Margin="4"
       SnapsToDevicePixels="True"
       RenderOptions.BitmapScalingMode="HighQuality"/>
            </Grid>
        </Border>

        <!-- Botones -->
        <Grid Grid.Row="3" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Left buttons -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left">
                <Button x:Name="BtnRetry"
            Style="{StaticResource MaterialDesignOutlinedButton}"
            Click="BtnRetry_Click"
            Margin="0,0,8,0">
                    <StackPanel Orientation="Horizontal">
                        <md:PackIcon Kind="Sync" Width="18" Height="18" Margin="0,0,6,0"/>
                        <TextBlock Text="Retry"/>
                    </StackPanel>
                </Button>

                <Button x:Name="BtnClear"
            Content="Clear"
            Style="{StaticResource MaterialDesignOutlinedButton}"
            Click="BtnClear_Click"/>
            </StackPanel>

            <Button Grid.Column="1" Content="Skip"
          Margin="8,0,0,0"
          Style="{StaticResource MaterialDesignOutlinedButton}"
          Click="BtnSkip_Click"/>

            <Button Grid.Column="2" x:Name="BtnDone"
          Content="✓  Done"
          Margin="8,0,0,0"
          Style="{StaticResource MaterialDesignRaisedButton}"
          Click="BtnDone_Click"/>
        </Grid>
    </Grid>
</Window>